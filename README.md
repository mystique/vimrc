# VIMRC

```bash
git clone https://github.com/mystique/vimrc.git
cd vimrc
git submodule update --init --recursive
```

```bash
# ycm install
apt-get install build-essential autoconf automake libtool clang python-dev cmake

./install.py --clang-completer --go-completer
```
